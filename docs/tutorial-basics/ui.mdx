import Pause from "../../src/components/ui/Pause"
import Stop from "../../src/components/ui/Stop"
import Volume from "../../src/components/ui/Volume"
import CountIn from "../../src/components/ui/CountIn"
import Loop from "../../src/components/ui/Loop"
import Metronome from "../../src/components/ui/Metronome"
import Layout from "../../src/components/ui/Layout"
import Zoom from "../../src/components/ui/Zoom"

# UI

## Header

- <Stop /> 停止
- <Pause /> 一時停止
- <Volume /> ドラッグ: 音量調節 クリック:ミュートまたはミュート解除
- <CountIn /> 再生直前に1小説待つ クリック: ON/OFF
- <Loop /> ループ クリック: ON/OFF
- <Metronome /> メトロノーム クリック: ON/OFF
- <Layout /> レイアウト変更 Page: 縦スクロール Horizontal: 横スクロール
- <Zoom /> 倍率変更